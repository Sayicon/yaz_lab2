spring:
  application:
    name: user-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://mongo-user:27017/userdb}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

server:
  port: 8082
