spring:
  application:
    name: product-service-test

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.0

management:
  endpoints:
    web:
      exposure:
        include: health

server:
  port: 0
